%%% Classically false things.

%positive A, B, C.
%negative X, Y, Z.


%refute (A => A) => A.
%refute (X => X) => X.

%refute ((B => A) => A) => A.
%refute ((Y => X) => X) => X.

%refute ~ ~ A.
%refute ~ ~ X.

%refute A + (B * C) => (A + B) * C.
%refute X + (Y * Z) => (X + Y) * Z.