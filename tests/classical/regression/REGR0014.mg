%positive A, B, C.
%negative X, Y, Z.

%refute ~ A + A.
%refute ~ (~ A + A).
%prove ~ ~ (~ A + A).

%refute ~ X + X.
%refute ~ (~ X + X).
%prove ~ ~ (~ X + X).

%refute ~ ~ A + ~ A.
%refute ~ (~ ~ A + ~ A).

%refute ~ ~ X + ~ X.
%refute ~ (~ ~ X + ~ X).