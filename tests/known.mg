% positive A.
%positive B.

%prove A => A.
% refute A.

% refute (\A x. \E y. p(x, y)) => \E y. \A x. p(x, y).
%prove (\A x. \E y. p(x, y)) <= \E y. \A x. p(x, y).

% refute A + ~A.
% refute ((A => C) => A) => A.
% refute (\E x. p(x)) <= ~~\E x. p(x).

%prove ~(A + B) <=> ~A & ~B.

%prove ~(A & B) <= ~A + ~B.
% refute  ~(A & B) => ~A + ~B.
